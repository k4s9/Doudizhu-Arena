# Required before adding or updating provider tokens from the Web UI.
# Use a stable, high-entropy value and keep it outside source control.
DOUDIZHU_CREDENTIAL_MASTER_KEY=replace-with-a-long-random-secret

# Provider tokens may still be referenced by agents.yaml environment placeholders.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
