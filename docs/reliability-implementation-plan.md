# 可靠性补强执行计划（2026-09-21）

基线 HEAD：3bb8d2bbd948f0e5291d3500af1b1ee9ebf34a88。开始时仅审阅材料未跟踪；该文档是静态设计，不是测试报告。适用规则为仓库根 AGENTS.md，无子目录规则。

本机没有 /home/k4s9，按实际工作目录备份至 /tmp/doudizhu-arena-backups/daily-2026-09-21/project（4.1 MB）；这是旧路径不可用时的临时备份，/tmp 不保证长期保留。不删除文件。Python 使用新建的 doudizhu-arena conda 环境。

## 依赖顺序与验收

|阶段|实现边界|验收|
|---|---|---|
|1 决策|兼容现有 Agent 协议，增加桌级决策及终态；沿用规则引擎|单 ID 关联 attempt/动作；首成功/重试成功/兜底/托管/取消/失败分离；领出必须出牌；超时合法兜底；按决策累计失败|
|2 事件（依赖1）|单进程广播、每连接独立队列、持久化顺序，不引入消息集群|多个订阅者事件一致；订阅与快照无缺口；重复忽略、缺口重取快照；慢观众隔离；回放按 seq|
|3 执行（依赖1）|冻结实际系统提示词、端点、采样参数、规则与源码指纹；凭据动态解析|同 manifest 可独立重跑；保留 task attempt 历史；并发调用前预留预算；未知 usage 保守占额；取消与遗留 running 恢复；双桌终局审计|
|4 报告（依赖1–3）|integrity.json、summary.json、metrics.csv、seed_level.csv、report.md|小矩阵 mock 完整比赛；NULL/零分母/超时/缺失关联边界；调用与决策分母独立；流局单列|
|5 对照与演示（依赖4）|单次/通用重试/规则反馈；固定观察与闭环分开；复用评测详情与回放|按来源 seed/match 划分；反馈组与通用组相同调用/token预算；工程失败前后证据；无 key 可重复演示|

## 不变边界

保留自定义四座位、三人实际出牌、AB 同牌双桌与差分计分。不集成 Mandol，不引入新 Agent 框架/多角色、反思记忆或规则工具牌手。兜底不计模型成功，同策略自对弈不用于宣称竞技优势。不承诺效果提升。

## 真实调用闸门

本次只授权本地实现与 mock；现有显式真实调用确认必须保留。示例 100 美元和 100 seeds × 20 hands × 3 variants 不构成授权。先完成可审阅 smoke 规格，要求实际模型、价格来源/生效日期、总预算和调用上限，再由用户确认。真实模型失败案例、usage 覆盖与策略效果在真实调用完成前均为未验证。

实际结果、运行步骤与未完成项见同目录 reliability-delivery.md（实施后生成）。
